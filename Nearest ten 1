#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,a;
    scanf("%d",&n);
    a=n%10;
    if(a!=0){
    if(a>=5){
        printf("%d",n+(10-a));
    }else{
        printf("%d",n-a);
    } }else{
        printf("%d",n);
    }
    return 0;}
