#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int n, o = 10;
scanf("%d", &n);
while(n != 0) {
    int d = n % 10;
    if(d < o) {
        o= d;
    }
    n /= 10;
}
printf("%d\n", o);
    return 0;
}
