#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n1, n2;
scanf("%d %d", &n1, &n2);
if(n1 != n2) {
    printf("Not Same\n");
    return 0;
}
int a1[n1], a2[n2];
for(int i = 0; i < n1; i++) {
    scanf("%d", &a1[i]);
}
for(int i = 0; i < n2; i++) {
    scanf("%d", &a2[i]);
}
int same = 1;
for(int i = 0; i < n1; i++) {
    if(a1[i] != a2[i]) {
        same = 0;
        break;
    }
}
if(same) {
    printf("Same\n");
} else {
    printf("Not Same\n");
}
    return 0;
}
