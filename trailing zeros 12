#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int num;
scanf("%d", &num);
int count = 0;
for(int i = 5; num / i >= 1; i *= 5) {
    count += num / i;
} 
printf("%d\n", count);  
    return 0;
}
